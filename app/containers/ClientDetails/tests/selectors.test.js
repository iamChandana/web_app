// import { selectClientDetailsDomain } from '../selectors';

describe('selectClientDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
