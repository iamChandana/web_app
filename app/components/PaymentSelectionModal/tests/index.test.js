// import React from 'react';
// import { shallow } from 'enzyme';

// import PaymentSelectionModal from '../index';

describe('<PaymentSelectionModal />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
